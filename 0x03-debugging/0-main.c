#include "main.h"

/**
 * main - tests function that tests  if integer is positive or negative
 * Return: 0
 */

int main(void)
{
int testValue;
testValue = 0;
positive_or_negative(testValue);
return (0);
}